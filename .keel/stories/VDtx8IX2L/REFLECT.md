# Reflect - VDtx8IX2L

## Acceptance Reflections

### 2026-03-14T21:55:00

Tightened the output contract for `keel mission next --status` to always show exactly 3 bullets and use concise text. Defined the deeper `keel mission next` exploration contract by including the status summary at the top followed by role-based guidance. Verified with `keel mission next --status` and `keel mission next`.
